// Crie um programa de contagem regressiva. Peça um número e conte deste número até 0, usando um loop while no console do navegador.

cont = prompt('digite um número para começar a contagem regressiva: ')

while(cont >= 0) {
    alert(cont)
    cont--
}