// Crie um programa de contagem progressiva. Peça um número e conte de 0 até esse número, usando um loop while no console do navegador.

num = prompt('digite um número para começar a contagem regressiva: ');

cont = 0;

while(cont <= num) {
    alert(cont)
    cont++
}